//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated from a template.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

using System;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using System.Collections.Specialized;
using System.ComponentModel;
using System.Globalization;
using System.Runtime.Serialization;

namespace ColpatriaSAI.Negocio.Entidades
{
    [DataContract(IsReference = true)]
    [KnownType(typeof(Moneda))]
    [KnownType(typeof(Premio))]
    [KnownType(typeof(TipoPremio))]
    [KnownType(typeof(TipoUnidadMedida))]
    public partial class UnidadMedida: IObjectWithChangeTracker, INotifyPropertyChanged
    {
        #region Primitive Properties
    
        [DataMember]
        public int id
        {
            get { return _id; }
            set
            {
                if (_id != value)
                {
                    if (ChangeTracker.ChangeTrackingEnabled && ChangeTracker.State != ObjectState.Added)
                    {
                        throw new InvalidOperationException("The property 'id' is part of the object's key and cannot be changed. Changes to key properties can only be made when the object is not being tracked or is in the Added state.");
                    }
                    _id = value;
                    OnPropertyChanged("id");
                }
            }
        }
        private int _id;
    
        [DataMember]
        public string nombre
        {
            get { return _nombre; }
            set
            {
                if (_nombre != value)
                {
                    _nombre = value;
                    OnPropertyChanged("nombre");
                }
            }
        }
        private string _nombre;
    
        [DataMember]
        public Nullable<int> tipounidadmedida_id
        {
            get { return _tipounidadmedida_id; }
            set
            {
                if (_tipounidadmedida_id != value)
                {
                    ChangeTracker.RecordOriginalValue("tipounidadmedida_id", _tipounidadmedida_id);
                    if (!IsDeserializing)
                    {
                        if (TipoUnidadMedida != null && TipoUnidadMedida.id != value)
                        {
                            TipoUnidadMedida = null;
                        }
                    }
                    _tipounidadmedida_id = value;
                    OnPropertyChanged("tipounidadmedida_id");
                }
            }
        }
        private Nullable<int> _tipounidadmedida_id;

        #endregion
        #region Navigation Properties
    
        [DataMember]
        public TrackableCollection<Moneda> Monedas
        {
            get
            {
                if (_monedas == null)
                {
                    _monedas = new TrackableCollection<Moneda>();
                    _monedas.CollectionChanged += FixupMonedas;
                }
                return _monedas;
            }
            set
            {
                if (!ReferenceEquals(_monedas, value))
                {
                    if (ChangeTracker.ChangeTrackingEnabled)
                    {
                        throw new InvalidOperationException("Cannot set the FixupChangeTrackingCollection when ChangeTracking is enabled");
                    }
                    if (_monedas != null)
                    {
                        _monedas.CollectionChanged -= FixupMonedas;
                    }
                    _monedas = value;
                    if (_monedas != null)
                    {
                        _monedas.CollectionChanged += FixupMonedas;
                    }
                    OnNavigationPropertyChanged("Monedas");
                }
            }
        }
        private TrackableCollection<Moneda> _monedas;
    
        [DataMember]
        public TrackableCollection<Premio> Premios
        {
            get
            {
                if (_premios == null)
                {
                    _premios = new TrackableCollection<Premio>();
                    _premios.CollectionChanged += FixupPremios;
                }
                return _premios;
            }
            set
            {
                if (!ReferenceEquals(_premios, value))
                {
                    if (ChangeTracker.ChangeTrackingEnabled)
                    {
                        throw new InvalidOperationException("Cannot set the FixupChangeTrackingCollection when ChangeTracking is enabled");
                    }
                    if (_premios != null)
                    {
                        _premios.CollectionChanged -= FixupPremios;
                    }
                    _premios = value;
                    if (_premios != null)
                    {
                        _premios.CollectionChanged += FixupPremios;
                    }
                    OnNavigationPropertyChanged("Premios");
                }
            }
        }
        private TrackableCollection<Premio> _premios;
    
        [DataMember]
        public TrackableCollection<TipoPremio> TipoPremios
        {
            get
            {
                if (_tipoPremios == null)
                {
                    _tipoPremios = new TrackableCollection<TipoPremio>();
                    _tipoPremios.CollectionChanged += FixupTipoPremios;
                }
                return _tipoPremios;
            }
            set
            {
                if (!ReferenceEquals(_tipoPremios, value))
                {
                    if (ChangeTracker.ChangeTrackingEnabled)
                    {
                        throw new InvalidOperationException("Cannot set the FixupChangeTrackingCollection when ChangeTracking is enabled");
                    }
                    if (_tipoPremios != null)
                    {
                        _tipoPremios.CollectionChanged -= FixupTipoPremios;
                    }
                    _tipoPremios = value;
                    if (_tipoPremios != null)
                    {
                        _tipoPremios.CollectionChanged += FixupTipoPremios;
                    }
                    OnNavigationPropertyChanged("TipoPremios");
                }
            }
        }
        private TrackableCollection<TipoPremio> _tipoPremios;
    
        [DataMember]
        public TipoUnidadMedida TipoUnidadMedida
        {
            get { return _tipoUnidadMedida; }
            set
            {
                if (!ReferenceEquals(_tipoUnidadMedida, value))
                {
                    var previousValue = _tipoUnidadMedida;
                    _tipoUnidadMedida = value;
                    FixupTipoUnidadMedida(previousValue);
                    OnNavigationPropertyChanged("TipoUnidadMedida");
                }
            }
        }
        private TipoUnidadMedida _tipoUnidadMedida;

        #endregion
        #region ChangeTracking
    
        protected virtual void OnPropertyChanged(String propertyName)
        {
            if (ChangeTracker.State != ObjectState.Added && ChangeTracker.State != ObjectState.Deleted)
            {
                ChangeTracker.State = ObjectState.Modified;
            }
            if (_propertyChanged != null)
            {
                _propertyChanged(this, new PropertyChangedEventArgs(propertyName));
            }
        }
    
        protected virtual void OnNavigationPropertyChanged(String propertyName)
        {
            if (_propertyChanged != null)
            {
                _propertyChanged(this, new PropertyChangedEventArgs(propertyName));
            }
        }
    
        event PropertyChangedEventHandler INotifyPropertyChanged.PropertyChanged{ add { _propertyChanged += value; } remove { _propertyChanged -= value; } }
        private event PropertyChangedEventHandler _propertyChanged;
        private ObjectChangeTracker _changeTracker;
    
        [DataMember]
        public ObjectChangeTracker ChangeTracker
        {
            get
            {
                if (_changeTracker == null)
                {
                    _changeTracker = new ObjectChangeTracker();
                    _changeTracker.ObjectStateChanging += HandleObjectStateChanging;
                }
                return _changeTracker;
            }
            set
            {
                if(_changeTracker != null)
                {
                    _changeTracker.ObjectStateChanging -= HandleObjectStateChanging;
                }
                _changeTracker = value;
                if(_changeTracker != null)
                {
                    _changeTracker.ObjectStateChanging += HandleObjectStateChanging;
                }
            }
        }
    
        private void HandleObjectStateChanging(object sender, ObjectStateChangingEventArgs e)
        {
            if (e.NewState == ObjectState.Deleted)
            {
                ClearNavigationProperties();
            }
        }
    
        protected bool IsDeserializing { get; private set; }
    
        [OnDeserializing]
        public void OnDeserializingMethod(StreamingContext context)
        {
            IsDeserializing = true;
        }
    
        [OnDeserialized]
        public void OnDeserializedMethod(StreamingContext context)
        {
            IsDeserializing = false;
            ChangeTracker.ChangeTrackingEnabled = true;
        }
    
        protected virtual void ClearNavigationProperties()
        {
            Monedas.Clear();
            Premios.Clear();
            TipoPremios.Clear();
            TipoUnidadMedida = null;
        }

        #endregion
        #region Association Fixup
    
        private void FixupTipoUnidadMedida(TipoUnidadMedida previousValue, bool skipKeys = false)
        {
            if (IsDeserializing)
            {
                return;
            }
    
            if (previousValue != null && previousValue.UnidadMedidas.Contains(this))
            {
                previousValue.UnidadMedidas.Remove(this);
            }
    
            if (TipoUnidadMedida != null)
            {
                if (!TipoUnidadMedida.UnidadMedidas.Contains(this))
                {
                    TipoUnidadMedida.UnidadMedidas.Add(this);
                }
    
                tipounidadmedida_id = TipoUnidadMedida.id;
            }
            else if (!skipKeys)
            {
                tipounidadmedida_id = null;
            }
    
            if (ChangeTracker.ChangeTrackingEnabled)
            {
                if (ChangeTracker.OriginalValues.ContainsKey("TipoUnidadMedida")
                    && (ChangeTracker.OriginalValues["TipoUnidadMedida"] == TipoUnidadMedida))
                {
                    ChangeTracker.OriginalValues.Remove("TipoUnidadMedida");
                }
                else
                {
                    ChangeTracker.RecordOriginalValue("TipoUnidadMedida", previousValue);
                }
                if (TipoUnidadMedida != null && !TipoUnidadMedida.ChangeTracker.ChangeTrackingEnabled)
                {
                    TipoUnidadMedida.StartTracking();
                }
            }
        }
    
        private void FixupMonedas(object sender, NotifyCollectionChangedEventArgs e)
        {
            if (IsDeserializing)
            {
                return;
            }
    
            if (e.NewItems != null)
            {
                foreach (Moneda item in e.NewItems)
                {
                    item.UnidadMedida = this;
                    if (ChangeTracker.ChangeTrackingEnabled)
                    {
                        if (!item.ChangeTracker.ChangeTrackingEnabled)
                        {
                            item.StartTracking();
                        }
                        ChangeTracker.RecordAdditionToCollectionProperties("Monedas", item);
                    }
                }
            }
    
            if (e.OldItems != null)
            {
                foreach (Moneda item in e.OldItems)
                {
                    if (ReferenceEquals(item.UnidadMedida, this))
                    {
                        item.UnidadMedida = null;
                    }
                    if (ChangeTracker.ChangeTrackingEnabled)
                    {
                        ChangeTracker.RecordRemovalFromCollectionProperties("Monedas", item);
                    }
                }
            }
        }
    
        private void FixupPremios(object sender, NotifyCollectionChangedEventArgs e)
        {
            if (IsDeserializing)
            {
                return;
            }
    
            if (e.NewItems != null)
            {
                foreach (Premio item in e.NewItems)
                {
                    item.UnidadMedida = this;
                    if (ChangeTracker.ChangeTrackingEnabled)
                    {
                        if (!item.ChangeTracker.ChangeTrackingEnabled)
                        {
                            item.StartTracking();
                        }
                        ChangeTracker.RecordAdditionToCollectionProperties("Premios", item);
                    }
                }
            }
    
            if (e.OldItems != null)
            {
                foreach (Premio item in e.OldItems)
                {
                    if (ReferenceEquals(item.UnidadMedida, this))
                    {
                        item.UnidadMedida = null;
                    }
                    if (ChangeTracker.ChangeTrackingEnabled)
                    {
                        ChangeTracker.RecordRemovalFromCollectionProperties("Premios", item);
                    }
                }
            }
        }
    
        private void FixupTipoPremios(object sender, NotifyCollectionChangedEventArgs e)
        {
            if (IsDeserializing)
            {
                return;
            }
    
            if (e.NewItems != null)
            {
                foreach (TipoPremio item in e.NewItems)
                {
                    item.UnidadMedida = this;
                    if (ChangeTracker.ChangeTrackingEnabled)
                    {
                        if (!item.ChangeTracker.ChangeTrackingEnabled)
                        {
                            item.StartTracking();
                        }
                        ChangeTracker.RecordAdditionToCollectionProperties("TipoPremios", item);
                    }
                }
            }
    
            if (e.OldItems != null)
            {
                foreach (TipoPremio item in e.OldItems)
                {
                    if (ReferenceEquals(item.UnidadMedida, this))
                    {
                        item.UnidadMedida = null;
                    }
                    if (ChangeTracker.ChangeTrackingEnabled)
                    {
                        ChangeTracker.RecordRemovalFromCollectionProperties("TipoPremios", item);
                    }
                }
            }
        }

        #endregion
    }
}
